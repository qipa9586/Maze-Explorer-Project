# 迷宫生成与寻路 — 课设计划书 v2.1

**版本路线**：
- **v1.0** ✅ — 迷宫核心：DFS 一锤子生成 + BFS 一锤子寻路 + 金线路径渲染，6 文件模块化
- **v1.5** ✅ — 算法可视化：`stepGenerate`/`stepSolve` 逐帧动画，状态机驱动，BFS 波前扩散
- **v1.7** ✅ — 玩家交互：WASD 手动走迷宫，H 键 BFS 提示，YOU WIN 步数对比，随机破环加岔路
- **v2.0** ✅ — 界面系统：手绘按钮主菜单，一键/动画双模式，三级难度选择，确认弹窗防误触
- **v2.1** ✅ — 工具栏速度控制 + 计时纪录 + 拖拽移动 + 存档
- **v2.2** ⏳ — 热力图✅ + 回溯动画✅ + 收集+评级✅ + 音效 + 玩家档案
- **v2.2** ⏳ — 热力图✅ + 回溯动画✅ + 收集+评级✅ + 音效✅ + 玩家档案✅ + 关于作者
- **v2.3** 💡 — 种子分享 + DFS 回溯高亮 + AI 演示 + 迷雾模式 + 每日挑战
- **v2.4** 💡 — A* 算法可视化 + 设置面板 + BFS vs A* 对比 + 步数曲线 + 操作回放

---

## 目录

- [技术选型](#技术选型)
- [文件结构](#文件结构)
- [核心数据结构](#核心数据结构)
- [算法要点](#算法要点)
  - [一、随机化 DFS 生成（显式栈版）](#一随机化-dfs-生成显式栈版)
  - [二、BFS 寻路](#二bfs-寻路)
  - [三、GUI 渲染（raylib）](#三gui-渲染raylib)
- [v1.5 — 算法可视化动画（已完成）](#v15-计划--算法可视化动画已完成)
  - [引导思路：while → 逐帧 + 持久栈 + 状态机](#v15-计划--算法可视化动画已完成)
  - [改动面一览](#v15-改动面一览)
- [v2.0 — 菜单 + 难度选择（已完成）](#v20--菜单--难度选择已完成)
  - [主菜单](#主菜单)
  - [双模式（instantMode）](#双模式instantmode)
  - [玩家模式（PLAYING）](#玩家模式playing)
  - [随机破环](#随机破环)
  - [确认弹窗](#确认弹窗)
  - [动态迷宫尺寸](#动态迷宫尺寸)
- [v2.1 — 速度控制 + 计时 + 存档](#v21--速度控制--拖拽--计时--存档)
- [v2.2 — 视觉增强 + 游戏性](#v22-计划--视觉增强--游戏性)

---

## 技术选型

| 事项 | 选择 |
|------|------|
| 语言 | C（C17） |
| 图形库 | raylib 5.5 |
| 编译器 | MinGW-w64 gcc 15.2 + gdb 16.3 |
| 生成算法 | 随机化 DFS（显式链表栈） |
| 寻路算法 | BFS（链表队列，最短路径） |
| 格子表示 | 结构体，四面墙 + visited |
| 存储方式 | 动态二维数组 `Cell **grid` |
| 代码组织 | 多文件模块化 |

---

## 文件结构

```
Maze-Explorer-Project/
├── src/
│   ├── maze.h          # 公共声明：结构体、常量、函数原型
│   ├── maze.c          # 迷宫创建/销毁
│   ├── generate.c      # 随机化 DFS 生成（含链栈实现）
│   ├── solver.c        # BFS 寻路 + 路径回溯（含链队实现）
│   ├── gui.c           # raylib 渲染：画迷宫 + 路径 + 标记
│   └── main.c          # 入口：初始化 → 生成 → 渲染 → 交互
├── .vscode/            # VS Code 调试配置
├── Study-Guides/       # 计划书 + 总结文档
└── tests/
    ├──test_raylib.c
    └──test_raylib.exe     # 环境验证
```

---

## v1.0 — 核心数据结构与算法

### Cell — 单个格子（v1.0）

```c
typedef struct {
    bool top, right, bottom, left;   // 四面墙，true=有墙挡住
    bool visited;                    // DFS生成和BFS寻路共用
    bool isPath;                     // true=在BFS最短路径上
    bool explored;                   // true=BFS探索过（v1.5加）
    bool playerPath;                 // true=玩家走过（v1.7加）
} Cell;
```

### Maze — 迷宫整体（各版本逐步追加字段）

```c
typedef struct {
    int rows, cols, cellSize;        // 行列数+动态格子尺寸（v2.0改为动态）
    int offsetX, offsetY;            // 居中偏移（v2.0从宏改为字段）
    Cell **grid;                     // 二维格子数组
    Position *path;                  // BFS回溯路径坐标
    int pathLen;                     // 路径长度
    MazeState state;                 // 状态机（v1.5加）
    StackNode *genStack;             // DFS动画栈（v1.5加）
    Queue *bfsQueue;                 // BFS动画队列（v1.5加）
    int **prevRow, **prevCol;        // BFS回溯数组（v1.5加）
    int playerRow, playerCol;        // 玩家位置（v1.7加）
    int playerStep;                  // 计步（v1.7加）
    bool instantMode;                // 一键/动画模式（v2.0加）
    MenuButton menuButton[3];        // 主菜单按钮（v2.0加）
    MenuButton sizeButton[3];        // 难度选择按钮（v2.0加）
    bool confirmReturn, confirmQuit; // 确认弹窗（v2.0加）
    SpeedButton speedButton[4];      // 速度档位按钮（v2.1加）
    int animSpeed;                   // 每帧步数（v2.1加）
    MenuButton saveButton;           // 存档按钮（v2.1加）
    double startTime;                // 计时起点（v2.1加）
    double elaspedTime;              // 实际用时，WON时存一次（v2.1加）
    double bestTime[3];              // 最佳纪录（v2.1加）
} Maze;
```

### 辅助结构

- **Position**：`{int col, row}` — 坐标（v1.0）
- **StackNode**：`{Position pos, StackNode *next}` — 链栈节点，哑头设计（v1.0）
- **QueueNode**：`{int row, col, prevRow, prevCol, *next}` — 链队节点（v1.0）
- **Queue**：`{QueueNode *front, *rear}` — 链队带尾指针 O(1) enqueue（v1.0）
- **MenuButton**：`{Rectangle bounds, const char *label}` — 纯手绘按钮（v2.0）
- **SpeedButton**：`{Rectangle bounds, int speed}` — 速度档位按钮（v2.1）

---

## 算法要点（各版本累积）

### 一、随机化 DFS 生成 — v1.0

```
方向数组：
  dr[4] = {-1,  1,  0,  0}
  dc[4] = { 0,  0, -1,  1}

显式栈（链表实现，哑头）：
  栈顶始终是"当前正在探索的格子"
  初始 push(0, 0)，标记 grid[0][0].visited = true
```

#### 主循环逻辑

```
(0, 0) 标记 visited，push 入栈

while (栈非空):
    current = peek 栈顶

    // 一：收集未访问邻居
    for i = 0..3:
        nr = current.row + dr[i]
        nc = current.col + dc[i]
        如果没越界且 grid[nr][nc].visited == false:
            unvisited 加入 i

    // 二：死胡同回溯
    if unvisited 为空:
        pop 栈顶; continue

    // 三：Fisher-Yates 洗牌随机选
    // 四：对称拆墙
    // 五：标记 visited，push 邻居
```

#### 关键提醒

- **Fisher-Yates 洗牌**：`srand(time(NULL))` 在 `main` 开头调一次
- **拆墙要对称**：当前格的右墙拆了，邻居的左墙也得拆
- **peek 和 pop 的区别**：peek 只看不删，pop 是死胡同才删（回溯）
- **栈空 = 生成完毕**
- **`nextRow`/`nextCol` 是循环最后一次的值**：洗牌后必须用 `dRows[chosen]`/`dCols[chosen]` 重新算

### 二、BFS 寻路 — v1.0（动画扩展 v1.5，双模式 v2.0）

**数据结构**：链队（`front`+`rear` 双指针），FIFO。`QueueNode` 含 row/col + prevRow/prevCol 用于回溯。

**流程**：
1. 重置全部 visited = false，起点 (0,0) 入队标记 visited
2. 偏移数组复用 `dRows/dCols`，与 DFS 生成共享
3. while 循环：dequeue 当前节点，若是终点 → break 进回溯。否则四个方向循环：
   - 越界检查 + 墙打通检查（当前格的对应方向 `== false`）+ 邻居未 visited
   - 三个条件都过 → 记录 `prevRow[nextR][nextC] = curr.row`、标记 visited、入队
4. 回溯：从终点沿 prev 倒回起点，每步设 `isPath = true`，同时存入 `maze->path[]` 数组

**为什么 BFS 保证最短路径**：无权图每步代价 = 1，BFS 层层扩散，第 k 层的格子距离起点恰为 k 步。谁先碰到终点谁最短——因为同一层所有格子距起点一样远，先到的层数最少。

**BFS vs DFS 生成 vs BFS 寻路**：生成只管 visited（挖通），寻路要同时看 visited + 墙是否打通（中间没墙挡才能走过去）。这是 BFS 寻路和 DFS 生成判断条件的最大区别。

**注意**：BFS 的 prev 数组需要在整个搜索期间持久化（不能随出队 free），所以用二维 `int **prevRow/prevCol` 而非 QueueNode 里的字段。

### 三、GUI 渲染 — v1.0 基础（菜单 v2.0，工具栏 v2.1）

**帧循环骨架**：`InitWindow → while(!WindowShouldClose()) { BeginDrawing; ...; EndDrawing; } → CloseWindow`

**居中公式**：`(总空间 - 总内容) / 2`。迷宫用 `offsetX/Y`，按钮组用 `startY`，同一条公式。

**画布分层**（从底到顶）：
1. `ClearBackground` — 清屏
2. `DrawRectangle` 格子底色（DARKGRAY）
3. 条件覆盖：`explored` → 深蓝紫，`isPath` → GOLD 填充
4. `DrawCircle` 起点(GREEN)/终点(RED) 小圆点
5. `DrawLine` 四面墙（WALL_COLOR 浅灰）
6. `DrawLineEx` BFS 最短路径金线（加粗 3px）
7. `DrawRectangle` 玩家橙色方块（PLAYING 状态）
8. UI 文字层：按键提示 / YOU WIN / 确认弹窗

**坐标系**：raylib 原点在窗口左上角，x 右、y 下。`(row, col)` 是逻辑坐标，`(x, y)` 是像素坐标。转换：`x = offsetX + col * cellSize`，`y = offsetY + row * cellSize`。

**自定义按钮**：`Rectangle` + `CheckCollisionPointRec` 判鼠标悬停变色 + `IsMouseButtonPressed` 判点击。无外部 UI 库，纯手绘。

**帧计数器调速**：`static int cooldown; if (cooldown++ % N == 0) { action; }`。玩家移动用 N=5（12步/秒），动画生成用 `for(i=0;i<maze->animSpeed;i++)`。详细 raylib API 参见 `raylib-函数速查手册.md`。

---

## v1.5 计划 — 算法可视化动画（已完成）

### 核心思路

v1.0 的 while 循环一口气跑到底 → v1.5 拆成 `stepGenerate()` / `stepSolve()`，每帧只执行一次循环体。帧循环跑 `BeginDrawing → drawMaze → EndDrawing`，每帧看到最新状态。

### 三个关键决策

**1. 栈/队列从局部变全局**：v1.0 函数局部变量出函数就销毁 → v1.5 挂 `Maze->genStack` / `Maze->bfsQueue`，跨帧存活。动画版用持久栈（`stepGenerate`），一键版保留局部栈（`generateRandomizedMaze`），两套不冲突。

**2. 状态机驱动**：用 `MazeState` 枚举 + `main.c` 里 `if/else` 分岔，替代散落 bool 变量。状态流转：IDLE → GENERATING → GENERATED → SOLVING → SOLVED。

**3. 每帧只跑一步**：`stepGenerate` 的 while 改成 `if (isEmpty) return true`，只跑一次迭代体。`stepSolve` 同理，每帧 dequeue 一个节点。加 `for(i=0; i<N; i++)` 包层实现可调速度。

### stepGenerate 实现

```c
bool stepGenerate(Maze *maze) {
    if (isStackEmpty(maze->genStack)) return true;  // 完成
    Position curr = peek(maze->genStack);
    // 收集未访问邻居 → 洗牌 → 拆墙 → push → return false
}
```

`return true` = 生成完毕，调用方切 `GENERATED`。`return false` = 只走了一步，下帧继续。

### stepSolve 实现

```c
bool stepSolve(Maze *maze) {
    if (isQueueEmpty(maze->bfsQueue)) return true;
    QueueNode curr; dequeue(maze->bfsQueue, &curr);
    if (curr 是终点) return true;
    // 四个方向：墙打通 + 未访问 → 记录 prev → 标记 visited+explored → enqueue
    return false;
}
```

BFS 探索过的格子设 `explored = true`，gui.c 用深蓝紫色渲染，形成"波前扩散"视觉。

### 改动面一览

| 文件 | 改动 |
|------|------|
| maze.h | 加 genStack、bfsQueue、explored、state、prevRow/Col 字段；stepGenerate/stepSolve 声明 |
| maze.c | createMaze 初始化 state=IDLE，genStack/bfsQueue=NULL，explored=false |
| generate.c | 保留原 generateRandomizedMaze（一锤子）；新增 stepGenerate（逐帧） |
| solver.c | 保留原 mazeSolver（一锤子）；新增 stepSolve（逐帧） |
| gui.c | explored 格子深蓝紫渲染；PLAYING 玩家方块；SOLVING 状态下画波前 |
| main.c | 状态机 if/else 驱动；R 键重新初始化栈；Space 键初始化队列切 SOLVING |

### 已踩坑

- **把 `pop(stack, ...)` 写成 `pop(maze->genStack, ...)`**：generateRandomizedMaze 局部栈被误改死循环，要命
- **prevRow/Col 初始化放 stepSolve 里**：每帧重置把前帧记录清空，移 main.c Space 键做一次
- **stepSolve 每帧 memset prev**：移走后仍残留 for 循环置 -1，全删

---

## v2.0 — 菜单 + 难度选择（已完成）

### 主菜单

程序启动进入 IDLE 状态。三个手绘按钮纵向排列，居中：

```
Animated Generation  → 初始化 genStack，逐帧 DFS 动画生成
Instant Generation   → generateRandomizedMaze 一锤子 + breakCycles
Player Exploration   → 进入难度选择二级菜单
```

**按钮实现**：`MenuButton` 结构体（bounds + label），raylib 无 UI 库全靠手绘。`CheckCollisionPointRec` 判鼠标悬停变色，`IsMouseButtonPressed` 判点击。居中公式：`startY = WIDTH/2 - (btnH*3 + gap*2)/2`。

### 双模式（instantMode）

`Maze->instantMode` 标志位：按钮 0 设 false（动画版），按钮 1 设 true（一键版）。空格寻路分岔——true 直接 `mazeSolver` 一锤子，false 走 `stepSolve` 逐帧动画。

### 玩家模式（PLAYING）

`IsKeyDown` 按住持续移动（每 5 帧一步），WASD+方向键双控。四面墙检查 + 越界检查。`H` 键静默 BFS 提示路径，再按关闭。走到终点 `WON` 状态显示 YOU WIN + 玩家步数 vs BFS 最短步数。

### 随机破环

DFS 生成完美迷宫后，`breakCycles(maze)` 随机拆约 1-2% 内部墙。只操作 row/col 在 1 到 rows/cols-2 的格子（不碰外墙），对称拆墙。打破"只有一条路"的结构，BFS 仍能找到最短。

### 确认弹窗

`confirmQuit` / `confirmReturn` 两个 bool。ESC 键 → 弹 "Quit? (Y/N)"。PLAYING 下 M 键 → 弹 "Return to difficulty? (Y/N)"。`SetExitKey(0)` 禁用 raylib 默认 ESC 退出。确认框画在 `EndDrawing` 前最顶层。

### 动态迷宫尺寸

`cellSize`/`offsetX`/`offsetY` 从宏改为 Maze 字段。`createMaze(rows, cols)` 自动计算：`cellSize = min(LENGTH/cols, WIDTH/rows)`，offset 用居中公式。三级难度选完调用 `destroyMaze` + `createMaze` 动态重建后按钮数组同步重初始化。

### 架构提升：状态机替代 bool 散旗

v1.5 引入 `MazeState` 枚举但只用 GENERATING/GENERATED/SOLVING/SOLVED 四个值。到 v2.0 扩展到 IDLE / SELECTING_SIZE / PLAYING / WON 后，单个 `maze->state` 字段完全取代了散落在各处的 bool 标记：

| 旧方案（散旗） | 新方案（状态机） |
|---------------|-----------------|
| `bool inMenu`, `bool inSizeSelect` | `state == IDLE`, `state == SELECTING_SIZE` |
| `bool isPlaying`, `bool won` | `state == PLAYING`, `state == WON` |
| 多处 if 判断组合爆炸 | 一路 `else if` 链，状态互斥不会冲突 |

**好处**：新增状态只需枚举加一项 + main.c 加一个分支，不会漏改某处 bool。gui.c 画什么也直接看 state 就行，不用记住"当前哪些 bool 同时为 true 代表什么含义"。后面 v2.1 加 `LOAD_OR_NEW` 也是加一行枚举 + 加一段画就行，证明了这个设计的可扩展性。

---

## v2.1 — 速度控制 + 拖拽 + 计时 + 存档

### v2.1.1 — 速度控制 ✅

不走按键，改为**顶部工具栏按钮**点击调速。工具栏统一放速度控制 + 存档 + 快捷键提示。

**引导问题**：顶部工具栏横跨窗口多宽？放在哪一行像素？按钮占多大？点下去后什么反应——改 animSpeed 然后按钮文字变化来反馈当前档位吗？

**改动清单**：
- `maze.h`：加 `MenuButton speedBtns[4]`（四个速度档位按钮）+ `int animSpeed` 档位值
- `maze.c`：`createMaze` 末尾初始化 toolbar 按钮坐标（横排，间隔小，放顶行）
- `gui.c`：新函数 `drawToolbar(Maze *maze)` — 画顶栏背景框 + 速度按钮 + 文字提示
- `main.c`：鼠标点击检测里加 `speedBtns[i]` 判断，设 `animSpeed`，让按钮显示"当前选中"状态

**工具栏布局示意**：

```
┌────────────────────────────────────────────────────────────────┐
│ [1x] [2x] [4x] [Max]  │  [Save]  │  R:Regen M:Menu Space:Solve│
└────────────────────────────────────────────────────────────────┘
```

速度按钮高亮当前选中（蓝底白字 vs 灰底黑字）。档位含义：

| 标签 | animSpeed | 效果 |
|------|-----------|------|
| 1x | 4 | 慢速动画 |
| 2x | 12 | 默认中速 |
| 4x | 24 | 快速 |
| Max | 9999 | 极速（近乎一锤子） |

**迷宫区偏移微调**：`offsetY` 计算时把 `TOOLBAR_H`（约 35-40px）从可用高度里扣掉，迷宫在下方居中。

#### drawToolbar 实现引导

**第一步：想想四个按钮和三个信息区分别画在哪**

顶栏全长 1600px，分成三段——左边速度区（300px），中间存档区（100px），右边快捷键提示（1200px 自由空间）。

```
x=0         x=300       x=400                    x=1600
├───────────┼───────────┼──────────────────────────┤
│ [1x][2x]  │  [Save]   │  R:Regen M:Menu Space   │
│  [4x][Max]│           │  H:Hint F12:Shot         │
└──────────────────────────────────────────────────┘
```

每个速度按钮宽约 50px，间距 10px，共四个。存档按钮也 50px。按钮的 y 坐标都在 5-8px 左右（离顶栏上边留一点边距），高度跟 TOOLBAR_H 差不多（约 25-30px）。

**第二步：怎么画当前选中的速度**

遍历四个速度按钮时，判断当前 `animSpeed` 是否等于这个按钮代表的速度值。是的话底色用蓝色（或亮色），不是的话灰底。文字始终黑色。`sprintf` 动态生成标签（"1x"/"2x"/"4x"/"Max"）。

**第三步：不同模式不同按钮**

工具栏内容按状态动态变化：

| 状态 | 显示内容 |
|------|---------|
| IDLE / SELECTING_SIZE | 不显示工具栏 |
| GENERATING（动画模式） | 速度按钮 [1x] [2x] [4x] [Max] + 快捷键提示 |
| GENERATED | 速度按钮（当前档位显示） + 快捷键提示 |
| SOLVING（动画模式） | 速度按钮 + 快捷键提示 |
| SOLVED | 快捷键提示（R:Regen M:Menu） |
| PLAYING | [Save] 按钮 + 快捷键提示（H:Hint M:Menu） |
| WON | 快捷键提示 |

速度按钮只在"动画正在跑"的状态有意义（GENERATING、SOLVING），`instantMode` 下显示但不可点（置灰）。存档按钮只出现在 PLAYING。

**第四步：按钮怎么存数据**

直接复用 MenuButton 也行，但最终选择单独建一个结构体，存速度值和标签更灵活：

```c
typedef struct {
    Rectangle bounds;
    int speed;
    const char *label;
} SpeedButton;
```

`maze.h` 声明，`maze.c`/`createMaze` 初始化四个档位按钮（1x/2x/4x/Max），`gui.c`/`drawToolbar` 遍历渲染 + 高亮当前选中。

**第四步：drawToolbar 放在哪个文件**

放 `gui.c`，`drawMaze` 前面调（或在 `drawMaze` 内部最开始调）。确保在 `BeginDrawing` 之后、迷宫格子之前调用。

**第五步：鼠标点击怎么处理**

`main.c` 里主循环的鼠标点击检测，在 `IDLE` / `SELECTING_SIZE` 判断后面加 toolbar 按钮的点击判断。遍历 `speedBtns[4]`，如果点中了某个按钮，就把 `maze->animSpeed` 设为它的 `speed` 值。存档按钮同理。

**第六步：显示哪些按钮取决于状态**

不是所有状态都显示工具栏。`IDLE` 和 `SELECTING_SIZE` 不显示——它们有自己的菜单界面。只在 `GENERATING` / `GENERATED` / `SOLVING` / `SOLVED` / `PLAYING` / `WON` 显示。速度快键只在动画模式（GENERATING、SOLVING）有意义。存档只在 PLAYING 有意义。这些都可以用 if 判断隐藏或置灰。

### v2.1.2 — 计时 + 最佳纪录 ✅

进 PLAYING 记 `maze->startTime = GetTime()`，到终点算耗时存入 `elaspedTime`（WON 时存一次，不每帧跑）。`bestTime[3]` 按难度存各档最佳，破纪录显示 "NEW BEST !"。

### v2.1.3 — 鼠标拖拽控制 ✅

**核心思路**：鼠标位置 vs 玩家屏幕坐标，差值超阈值即移动，玩家追鼠标 = "锁死在方块上"的手感。

**实现细节**：
- 参照点：玩家方块中心像素坐标（`offsetX + playerCol * cellSize + cellSize / 2`），每帧重算
- `dx = mouse.x - centerX`，`dy = mouse.y - centerY`
- `|dx| > |dy|` 走横向，否则纵向；需同时 `> threshold`（`cellSize / 2`）
- `dragDelay` 冷却 4 帧防鬼畜
- 碰撞箱判定：`IsMouseButtonPressed` + `CheckCollisionPointRec(playerRect)` 启动拖拽，防工具栏误触
- `dragging` 标记保持拖拽状态，松开鼠标清零
- 手型光标：`SetMouseCursor(MOUSE_CURSOR_POINTING_HAND)` 悬停玩家方块时切换（gui.c drawMaze 内实现，拖拽逻辑无关）
- 防首次误触：`prevState` 跟踪上帧状态，刚切到 PLAYING 时跳过拖拽

### v2.1.5 — 存档功能 ✅

**文件命名**：按尺寸区分，三个存档槽：

| 难度 | 尺寸 | 文件名 |
|------|------|--------|
| SMALL | 20×26 | `save_small.bin` |
| MEDIUM | 40×52 | `save_medium.bin` |
| LARGE | 60×78 | `save_large.bin` |

**二进制格式**（`fwrite`/`fread`）：

| 顺序 | 字段 | 类型 | 字节 |
|------|------|------|------|
| 1 | rows | int | 4 |
| 2 | cols | int | 4 |
| 3 | playerRow | int | 4 |
| 4 | playerCol | int | 4 |
| 5 | playerStep | int | 4 |
| 6 | elapsedTime | double | 8 |
| 7+ | 每格 5 个 bool（top, right, bottom, left, playerPath） | bool × rows × cols × 5 | — |

**函数签名**（放 `maze.c`）：

```c
bool saveGame(Maze *maze, const char *filename);   // true=成功
bool loadGame(Maze *maze, const char *filename);   // true=成功（文件存在且合法）
```

**存**：点 SAVE 按钮 → `saveGame()` → 显示 "Saved!"（半透明矩形 + DrawText），1.5 秒自动消失

**读的流程**：选大小后插入中间状态 `LOAD_OR_NEW`，仿 `confirmReturn`/`confirmQuit` 套路：

```
在 SELECTING_SIZE 点按钮:
  destroyMaze + createMaze(尺寸)
  fopen(filename, "rb") 试文件是否存在
  ├─ 存在 → state = LOAD_OR_NEW，maze->loadFilename = filename
  │         弹窗 "Continue saved game? (Y/N)"
  │           Y → loadGame, startTime = GetTime() - elapsedTime, state = PLAYING
  │           N / ESC → generate + breakCycles, startTime = GetTime(), state = PLAYING
  └─ 不存在 → generate + breakCycles, startTime = GetTime(), state = PLAYING
```

**新增字段**：
- `MazeState LOAD_OR_NEW` — 状态机枚举加一项
- `Maze.confirmLoad` — bool，控制弹窗显示
- `Maze.loadFilename` — const char*，存要读的档名

**MEDIUM 修复**：之前 i==1 分支只调 `generateRandomizedMaze` 没 `destroyMaze`+`createMaze`，导致迷宫尺寸保持上次的。现在三个分支统一：先 destroy 再 create 对应尺寸。

**grid 动态分配**：`saveGame` 已知 rows/cols 直接写。`loadGame` 先读 rows/cols，再 `free` 旧 grid + `malloc` 新 grid 后读入每格数据。`breakCycles` 改过的墙也一并存进去，不重新生成。

---

### 开发顺序（v2.1）

1. 速度控制 ✅
2. 计时 ✅
3. 鼠标拖拽 ✅
4. 存档 ✅

---

## v2.2 计划 — 视觉增强 + 游戏性

### v2.2.1 — BFS 热力图 ✅

BFS 探索过的格子按距起点步数着色——近蓝远红，展示算法"波前推进"。

**数据结构**：`int **dist` 二维数组，放 Maze 字段（与 `prevRow`/`prevCol` 一致）。`createMaze` 初始 `NULL`，`main.c` Space 键处 `malloc`，`destroyMaze` 里 `free`。

**stepSolve 改动**：
- 起点入队时设 `dist[0][0] = 0`
- 探索邻居时 `dist[nr][nc] = dist[curr.row][curr.col] + 1`
- `dist` 只记层数，不影响现有 `visited`/`explored`/`prevRow`/`prevCol` 逻辑

**gui.c 渲染**：
- `explored` 为 true 的格子，用 `dist` 值映射颜色
- `ColorFromHSV(dist * step, 1.0f, 1.0f)` → hue 从 240（蓝）渐变到 0（红）
- `step = 360.0f / maxDist`，确保颜色范围覆盖整个色相环
- 凉色（近）= 高 hue，暖色（远）= 低 hue

### v2.2.2 — 路径回溯动画 ✅

金线/紫底从迷宫终点一格一格往回亮，作为 SOLVING 到 SOLVED 的过渡动画。

**新增字段**：
- `int visiblePathLen` — Maze 字段，当前可见的路径格子数（终点→起点方向增长）
- `MazeState BACKTRACK_ANIM` — 回溯动画状态，SOLVING 结束后的中间态

**流程**：
```
stepSolve 返回 true → path[]/pathLen 已填满 → 回溯循环执行（不动）
  ├─ visiblePathLen = 1
  ├─ state = BACKTRACK_ANIM
  └─ continue
BACKTRACK_ANIM 块（main.c）：
  ├─ 每 N 帧 visiblePathLen++
  ├─ visiblePathLen >= pathLen → state = SOLVED
  └─ continue
```

**gui.c 改动**：紫底和金线循环 `for (i = 0; i < maze->visiblePathLen; i++)`，动画跑完 `visiblePathLen == pathLen` 自动全亮。

**动画方向**：`path[0]` 是终点、`path[pathLen-1]` 是起点——`visiblePathLen` 从 1 增长 = 金线从终点往起点"逆流"。符合实际回溯过程的视觉方向。

### v2.2.3 — 收集挑战 + 速通评级 ✅

收集品 + 竞速合为一体：金币只放死胡同尽头，全收集才能通关；通关后按步数评级（S/A/B/C）。

**三级菜单流程**：

```
IDLE → SELECTING_SIZE → SELECTING_MODE → PLAYING
         S/M/L              Normal / Challenge
```

- **Normal**：不放金币，终点直接 WIN，无评级
- **Challenge**：放金币，全收集才放行，通关后评级

**新增**：
- `MazeState SELECTING_MODE`
- `bool maze->challengeMode` — true=挑战模式
- `MenuButton modeButton[2]` — "Normal" / "Challenge"
- `createMaze` 初始化两个按钮坐标（仿 sizeButton 布局，两个按钮更宽松）
- `main.c`：`SELECTING_SIZE` 选完大小 go 到 `SELECTING_MODE`（三个分支末尾改 `state = SELECTING_MODE`，存档检测逻辑保留）；新增 `SELECTING_MODE` 点击判断——按钮 0 设 `challengeMode = false`，按钮 1 设 `challengeMode = true`，然后 `generate + breakCycles + (challengeMode ? placeItems : skip) + state = PLAYING + startTime = GetTime()`
- `gui.c`：`SELECTING_MODE` 状态画两个按钮 + 标题 "Choose Mode"

**新增字段**：
- `Cell.hasItem` — 是否有未收集金币
- `Maze.totalItems` — 金币总数（死胡同数，自动统计）
- `Maze.collectedCount` — 已收集数
- `Maze.challengeMode` — true=挑战模式

**金币投放**（`generate.c`，`breakCycles` 之后）：

```c
void placeItems(Maze *maze);
```

遍历所有格子（跳过起点/终点）：只有一面墙 open 的格子 = 死胡同，放金币。总数自然落在 15-30。

**渲染**（`gui.c`）：
- 金币：双层叠圆（外金内白）+ `$` 符号，紫底之后墙之前
- Toolbar：`Items: N/T` + 金币图标

**通关检查**（`main.c` PLAYING 终点处）：
- Normal → 直接 WON
- Challenge 且 `collectedCount < totalItems` → 弹提示 "Collect all items first!"
- Challenge 全收集 → WON + 评级

**评级**（WON 时计算）：

| 评级 | 条件 | 颜色 |
|------|------|------|
| S | steps <= shortestSteps × 1.1 | GOLD |
| A | steps <= shortestSteps × 1.5 | GREEN |
| B | steps <= shortestSteps × 2.0 | YELLOW |
| C | 其余 | WHITE |

WON 界面在 "Your Steps" 下方显示评级。

**存档兼容**：增 3 个字段——`hasItem`/`totalItems`/`collectedCount`。`saveGame` 格子 5 bool → 6 bool，头多写 2 个 int。旧档不兼容，课设级别可以接受。

### v2.2.4 — 音效 ✅

raylib `InitAudioDevice` + `LoadSound` + `PlaySound`，每个事件挂一个 wav。

**事件 → 音效**：

| 事件 | 触发位置 | 音效 |
|------|---------|------|
| 走一步 | PLAYING 移动成功 | 短促轻咚 |
| 撞墙 | 移动失败（有墙挡） | 闷响 |
| 收集金币 | hasItem → false | 清脆叮 |
| 全收集 | collectedCount == totalItems | 胜利叮 |
| WIN | 到终点通关 | 欢快短曲 |
| 按钮点击 | 三个菜单里点按钮 | 轻 click |

**初始化**（`main.c` 开头）：`InitAudioDevice();` + `LoadSound` 全部预加载。

**清理**（`main.c` 末尾）：`UnloadSound` → `CloseAudioDevice();`

**音效文件**：项目内建 `assets/` 目录存 .wav，或从 raylib 自带示例音效里抠。不依赖外部字体/音效路径，保证跨机可运行。

**文件改动**：
- `maze.h`：加 `Sound walkSound, wallSound, collectSound, winSound, clickSound;` 字段（或独立数组）
- `maze.c createMaze`：LoadSound 全部
- `maze.c destroyMaze`：UnloadSound + CloseAudioDevice

### v2.2.5 — 玩家档案与记录 ✅

持久化玩家数据到 `player_profile.bin`，跨 session 保留最佳纪录和游戏统计。

**新增结构体**：

```c
typedef struct {
    int rows, cols;
    int steps;
    double time;
    char grade;       // 'S','A','B','C'
    bool challengeMode;
    bool won;
} GameRecord;

#define MAX_HISTORY 20
```

**Maze 新增字段**：
- `int gamesPlayed` — 总局数
- `int gamesWon` — 胜场数
- `GameRecord history[MAX_HISTORY]` — 最近 20 局
- `int historyCount` — 已记录局数（环形写入）

**player_profile.bin 格式**：

| 字段 | 类型 |
|------|------|
| bestTime[3] | double ×3 |
| gamesPlayed | int |
| gamesWon | int |
| totalSteps | int |
| historyCount | int |
| history | GameRecord × historyCount |

**入口**：
- IDLE 菜单加第四个按钮 "Player Profile" → 新状态 `PROFILE`，展示统计面板 + 最近记录列表
- 启动时 `main.c` 开头检查 `player_profile.bin` 是否存在 → 存在就读入，bestTime 立即可用

**WON 时写入**（`main.c` WON 处）：
- `gamesPlayed++`
- 如果通关：`gamesWon++`
- 构造 GameRecord，环形写入 history
- 更新 bestTime
- 调用 `saveProfile(maze)` 写盘

**Profile 界面**（`gui.c`，`PROFILE` 状态）：

```
┌──────────────────────────────────┐
│         PLAYER PROFILE           │
│                                  │
│  Games: 23    Won: 18 (78%)     │
│  Best S: 12.3s   Best M: 45.7s  │
│  Best L: 98.2s                   │
│                                  │
│  Recent Games:                   │
│  #1 M | 142 steps | A | 38.2s ✓ │
│  #2 S | 89 steps  | S | 10.1s ✓ │
│  #3 L | 312 steps | C | 112s  ✗ │
│  ...                             │
│                                  │
│     [Back to Menu]               │
└──────────────────────────────────┘
```

**与存档系统的关系**：
- `player_profile.bin` = 跨 session 的全局数据（bestTime / 历史记录）
- `save_*.bin` = 单次游戏存档（迷宫状态 / 玩家位置）
- 两者独立，各读各的

**新增状态**：`MazeState PROFILE`（含一个返回按钮 + ESC/M/Backspace 快退）。

**Recent Games 滚轮滚动**：记录超过 5 条时，用 `GetMouseWheelMove()` 偏移 `scrollOffset`：

```c
static int scrollOffset = 0;

int wheel = GetMouseWheelMove();
scrollOffset -= wheel;
if (scrollOffset < 0) scrollOffset = 0;
int maxOffset = maze->historyCount - 5;
if (maxOffset < 0) maxOffset = 0;
if (scrollOffset > maxOffset) scrollOffset = maxOffset;

int start = scrollOffset;  // 从第 start 条开始画 5 条
for (int i = 0; i < 5 && start + i < maze->historyCount; i++) {
    GameRecord *G = &maze->history[start + i];
    // 画一条
}
```

右侧画滚动条：`DrawRectangle(x, barY, 8, scrollBarHeight, GRAY)` 指示当前位置。

**Profile 关键修复**：
- `createMaze` 改 `calloc` 避免 malloc 未初始化 historyCount 垃圾值
- 所有 `createMaze` 后紧跟 `loadProfile(maze)` 保持 bestTime/历史跨局持久
- bestTime 更新逻辑从 gui.c 移到 main.c WON 处（在 `saveProfile` 之前），确保最新记录先落盘

---

### v2.2.6 — 关于作者（右下角） ⏳

右下角固定显示作者信息区——校徽 + 姓名 + 学号 + 班级。raylib `LoadImageFromMemory` 将校徽编译进 exe。

**第一步：校徽转 C 数组**

```c
// export_logo.c — 跑一次生成 school_logo.h
#include <raylib.h>
int main() {
    Image img = LoadImage("school_logo.png");
    ExportImageAsCode(img, "school_logo.h");  // raylib 5.5 内置
    UnloadImage(img);
}
```

生成 `school_logo.h`：`unsigned char schoolLogoData[] = {...};` + `int schoolLogoSize = ...;`

**第二步：Maze 加字段 + LoadTexture**

- `maze.h`：`Texture2D schoolLogoTex;`
- `maze.h`：加 `int logoWidth, logoHeight;` 控制显示尺寸
- `main.c` 开头 `#include "school_logo.h"`
- `createMaze` 或 main.c 初始化：`LoadImageFromMemory → LoadTextureFromImage → UnloadImage`
- 缩放校徽到合适大小（约 80×80 或按比例）

**第三步：DrawTexture**

在 `drawMaze` 或独立区域右下角固定位置：

```c
int logoX = LENGTH - maze->logoWidth - 20;
int logoY = WIDTH - maze->logoHeight - 20;
DrawTextureEx(maze->schoolLogoTex, (Vector2){logoX, logoY}, 0, 1, WHITE);

// 文字靠左排列
DrawText("Zhang Haomin (张昊民)", logoX - 280, logoY + 10, 20, WHITE);
DrawText("25 Computer Science Class 1", logoX - 280, logoY + 38, 18, LIGHTGRAY);
DrawText("Student ID: 20250320509", logoX - 280, logoY + 60, 16, GRAY);
DrawText("Longyan University", logoX - 280, logoY + 80, 16, GRAY);
```

**第四步：destroyMaze 清理**

```c
UnloadTexture(maze->schoolLogoTex);
```

**跨机运行**：校徽编入 maze.exe，不需要 png 文件。编译时 `school_logo.h` 必须存在，运行时零依赖。

---

## v2.3 计划 — 分享 + 挑战 + 演示

### v2.3.1 — 种子分享

工具栏显示当前随机种子，输入种子可复现同一迷宫——挑战"同迷宫谁更快"。

### v2.3.2 — DFS 回溯高亮

stepGenerate pop 时格子短暂闪红——展示算法"探了一圈退回来"。

### v2.3.3 — AI 自动演示

按 A 键 AI 沿 BFS 路径自动走——答辩一键展示最优解。

### v2.3.4 — 迷雾模式

只显示玩家周围 2-3 格范围，其余全黑。走过的地方留痕迹但不透明的。靠记忆摸路——难度翻倍。`Cell.visible` 字段控制。

### v2.3.5 — 每日挑战

在玩家模式的难度选择界面（SELECTING_SIZE）加第四个按钮：`Daily Challenge`。`seed = 日期转数字`（如 20260605），固定尺寸 Medium(40×52)。同一天所有人打开同一个迷宫，排行榜/对比步数更有趣。

---

## v2.4 计划 — 设置 + A* + 数据分析

### 开发顺序

1. A* 算法可视化
2. BFS vs A* 对比模式
3. 设置面板（音量/窗口）
4. 操作回放
5. 步数评分曲线

---

### v2.4.1 — A* 算法可视化

BFS 排队列先进先出——谁先到先处理谁。A* 用**优先队列**——距离短的优先处理。启发函数 `h = |r - endR| + |c - endC|` 曼哈顿距离。朝向终点方向优先探索。新增 `stepAStar`，需要优先队列（最小堆）。

**与 BFS 热力图的对比效果**：BFS 圆形 360° 扩散，A* 指→终点窄锥形，探索格子少 40-60%。

---

### v2.4.2 — BFS vs A* 对比模式

同一迷宫左侧 BFS 热力图、右侧 A* 热力图，同时跑。`doubleBuffer` 双渲染区（各占半屏），肉眼比较探索效率差异。

在 IDLE 菜单加 "Algorithm Comparison" 按钮切到此模式。

---

### v2.4.3 — 设置面板

右上角 `⚙` 字符（DrawText），点击弹出半透明设置面板：

- 主音量滑块 → `SetMasterVolume`
- （可选）窗口大小缩放

设置存 `settings.bin`（fwrite/fread），启动时读取。面板 ESC 或点齿轮关闭。

新增状态 `SETTINGS`，暂停游戏但不切回 IDLE（在 PLAYING 中也能开）。

---

### v2.4.4 — 操作回放

WON 后按 R：自动回放玩家的每一步操作。

**数据结构**：`int replaySteps[10000]` + `int replayCount`。PLAYING 时每走一步 push 方向码（0=上 1=下 2=左 3=右），WON 后按 R 逐帧自动化操作。

**回放状态**：`REPLAY` — 玩家方块自动动，每 0.2s 一步，录屏级别展示效果。

---

### v2.4.5 — 步数评分曲线

Player Profile 面板加折线图——最近 20 局的步数走势。

**数据结构**：在 `player_profile.bin` 的 `GameRecord` 里画出来。X 轴 = 局序号，Y 轴 = 步数，raylib `DrawLine` 连点 + `DrawCircle` 标点。

同一张图叠加最优步数参考线（`pathLen-1`），直观看出"每局比最短多绕了多少"。
