# 迷宫生成与寻路 — 课设计划书 v2.0

**版本路线**：
- v1.0 ✅ — DFS 一锤子生成 + BFS 一锤子寻路 + 金线路径
- v1.5 ✅ — DFS 逐帧动画 + BFS 波前扩散动画 + 状态机驱动
- v1.7 ✅ — 玩家手动模式（WASD/方向键/H提示/YOU WIN） + 随机破环
- v2.0.3 ✅ — 菜单系统（主菜单 + 按钮 + IDLE 状态 + 一键/动画双模式）
- v2.0.4 ✅ — 难度选择（玩家模式二级菜单：Small/Medium/Large）
- v2.0.5 ⏳ — 速度控制（1/2/3/4 键调速）

---

## 目录

- [技术选型](#技术选型)
- [文件结构](#文件结构)
- [核心数据结构](#核心数据结构)
- [算法要点](#算法要点)
  - [一、随机化 DFS 生成（显式栈版）](#一随机化-dfs-生成显式栈版)
  - [二、BFS 寻路](#二bfs-寻路)
  - [三、GUI 渲染（raylib）](#三gui-渲染raylib)
- [v1.5 计划 — 算法可视化动画（已完成）](#v15-计划--算法可视化动画已完成)
- [v2.0 计划 — 交互增强](#v20-计划--交互增强)
  - [v2.0.1 玩家手动走迷宫 + 随机破环](#v201--玩家手动走迷宫player-模式)
  - [v2.0.3 菜单系统](#v203--菜单系统已完成)
  - [v2.0.4 难度选择](#v204--难度选择已完成)
  - [v2.0.5 速度控制](#v205--速度控制待定)
  - [后续想法](#后续想法)

---

## 技术选型

| 事项 | 选择 |
|------|------|
| 语言 | C（C17） |
| 图形库 | raylib 5.5 |
| 编译器 | MinGW-w64 gcc 15.2 + gdb 16.3 |
| 生成算法 | 随机化 DFS（递归回溯） |
| 寻路算法 | BFS（最短路径） |
| 格子表示 | 结构体，四面墙 + visited |
| 存储方式 | 动态二维数组 `Cell **grid` |
| 代码组织 | 多文件模块化 |

---

## 文件结构

```
D:\Data-Structure-And-Algorithm\Maze\
├── src/
│   ├── maze.h          # 公共声明：结构体、常量、函数原型
│   ├── maze.c          # 迷宫创建/销毁/重置（公共操作）
│   ├── generate.c      # 随机化 DFS 生成
│   ├── solver.c        # BFS 寻路 + 路径回溯
│   ├── GUI.c           # raylib 渲染：画迷宫/动画/交互
│   └── main.c          # 入口：初始化 → 生成 → 渲染 → 寻路 → 结束
├── .vscode/            # 调试配置（已完成）
├── Study-Guides/
│   └── 迷宫课设计划书.md
└── tests/
    ├──test_raylib.c
    └──test_raylib.exe     # 环境验证（已完成）
```

---

## 核心数据结构

### Cell — 单个格子

```c
typedef struct {
    bool top;       // 上墙：true = 存在（不通），false = 打通
    bool right;     // 右墙
    bool bottom;    // 下墙
    bool left;      // 左墙
    bool visited;   // DFS 生成 + BFS 寻路共用（可重置）
} Cell;
```

- 初始状态：四面墙全部 `true`
- DFS 打通相邻两格：各自对应方向的墙都设 `false`
- `visited`：生成阶段标记"已被挖通"；寻路前全部重置为 `false`

### Maze — 迷宫整体

```c
typedef struct {
    int rows;       // 行数（高度，格子数）
    int cols;       // 列数（宽度，格子数）
    Cell **grid;    // 动态二维数组，grid[r][c]
} Maze;
```

### BFS 辅助 — 队列节点

```c
typedef struct {
    int row;
    int col;
    int prevRow;    // 回溯用：从哪一格来的
    int prevCol;
} QueueNode;
```

---

## 算法要点

### 一、随机化 DFS 生成（显式栈版）

#### 数据结构准备

```
方向数组：
  dr[4] = {-1,  1,  0,  0}    // 上、下、左、右 → 行的变化量
  dc[4] = { 0,  0, -1,  1}    // 上、下、左、右 → 列的变化量

显式栈（链表实现，哑头）：
  栈顶始终是"当前正在探索的格子"
  初始 push(0, 0)，标记 grid[0][0].visited = true
```

#### 主循环逻辑（伪代码）

```
(0, 0) 标记 visited，push 入栈

while (栈非空):
    current = peek 栈顶          // 看当前格，不出栈

    // 第一步：收集未访问邻居
    unvisited = []
    for i = 0..3:               // 四个方向
        nr = current.row + dr[i]
        nc = current.col + dc[i]
        如果 (nr, nc) 没越界 且 grid[nr][nc].visited == false:
            unvisited 加入 i    // 记录方向编号

    // 第二步：分支判断
    if unvisited 为空:
        pop 栈顶                 // 死胡同，回溯
        continue

    // 第三步：随机选一个未访问邻居
    用 Fisher-Yates 洗牌打乱 unvisited 的顺序
    取第一个作为 chosen 方向

    // 第四步：拆墙（两格各改一次）
    根据 chosen 方向：
        chosen == 0（上）: grid[r][c].top    = false; grid[nr][nc].bottom = false;
        chosen == 1（下）: grid[r][c].bottom = false; grid[nr][nc].top    = false;
        chosen == 2（左）: grid[r][c].left   = false; grid[nr][nc].right  = false;
        chosen == 3（右）: grid[r][c].right  = false; grid[nr][nc].left   = false;

    // 第五步：进入邻居
    grid[nr][nc].visited = true
    push(nr, nc)
```

#### 手动走一遍（2×2 迷宫，4 格）

```
初始：四格都四面有墙，visited 全 false
栈 = [(0,0)]，grid[0][0].visited = true

=== 第 1 轮 ===
栈顶 (0,0)，邻居：右(0,1) 未访问，下(1,0) 未访问
随机选 → 右
拆墙：(0,0).right=false, (0,1).left=false
push(0,1)，grid[0][1].visited = true
栈 = [(0,0), (0,1)]

=== 第 2 轮 ===
栈顶 (0,1)，邻居：左(0,0) 已访问，下(1,1) 未访问
只有下 → 选中
拆墙：(0,1).bottom=false, (1,1).top=false
push(1,1)，grid[1][1].visited = true
栈 = [(0,0), (0,1), (1,1)]

=== 第 3 轮 ===
栈顶 (1,1)，邻居：上(0,1) 已访问，左(1,0) 未访问
只有左 → 选中
拆墙：(1,1).left=false, (1,0).right=false
push(1,0)，grid[1][0].visited = true
栈 = [(0,0), (0,1), (1,1), (1,0)]

=== 第 4 轮 ===
栈顶 (1,0)，邻居：上(0,0) 已访问，右(1,1) 已访问
无未访问邻居 → pop(1,0)
栈 = [(0,0), (0,1), (1,1)]

接下来每轮栈顶都没有未访问邻居，逐个 pop 直到栈空 → 生成完毕
```

#### 画图理解墙是如何拆的

```
初始：              第1轮（(0,0)拆右墙）：  第2轮（(0,1)拆下墙）：  第3轮（(1,1)拆左墙）：
 ─ ─  ─ ─          ─ ─     ─ ─          ─ ─     ─ ─          ─ ─     ─ ─
│      │           │        │           │        │           │        │
│  起  │     →     │  起 ○  │     →     │  起 ○  │     →     │  起 ○  │
│      │           │        │           │  ○     │           │  ○  ○  │  
 ─ ─  ─ ─          ─ ─  ─  ─ ─          ─ ─  ─  ─ ─          ─ ─  ─  ─ ─
│      │           │        │           │        │           │        │
│      │           │        │           │  ○     │           │  ○  ○  │
│      │           │        │           │  终    │           │  终    │
 ─ ─  ─ ─          ─ ─  ─  ─ ─          ─ ─  ─  ─ ─          ─ ─     ─ ─
```

访问顺序：(0,0) → (0,1) → (1,1) → (1,0)，连通路径为 Z 字形，四周外墙保留。

#### 关键提醒

- **Fisher-Yates 洗牌**：`srand(time(NULL))` 在 `main` 开头调一次即可
- **拆墙要对称**：当前格的右墙拆了，邻居的左墙也得拆——不然走过去是通的，走回来撞墙
- **peek 和 pop 的区别**：peek 只看不删（还有未访问邻居时留着它继续挖），pop 是死胡同才删（回溯）
- **栈空 = 生成完毕**：所有格子都被访问过，每个连通，墙也拆好了
- **`nextRow`/`nextCol` 是循环最后一次的值**：for 循环收集邻居时 `nextRow`/`nextCol` 不断被覆盖，循环结束留在方向 3（右）。洗牌选中 `chosen` 后必须用 `dRows[chosen]`/`dCols[chosen]` 重新算一遍，否则拆的是错位的墙，定向隔壁却去了别人的邻居。

### 二、BFS 寻路

1. 起点 (0, 0)，终点 (rows-1, cols-1)
2. 队列初始 push 起点，标记 visited
3. 循环出队：
   - 若当前 == 终点 → 回溯路径
   - 否则：检查四个方向
     - 方向对应的墙必须为 `false`（没有墙挡着）
     - 邻居未被 visited
     - 满足则入队，记录 `prevRow/prevCol`（从哪来的）
4. BFS 的 visited 只标记"已入队/已探索"，和墙的打通无关

**路径回溯**：终点顺着 `prevRow/prevCol` 一路倒回起点，把路径上的格子标记出来用于渲染。

#### 主逻辑（伪代码）
```c
void mazeSolver(Maze *maze) {
    // 1. 重置 visited
    // 2. 创建队列，起点入队，标记 visited
    // 3. 偏移数组 dRows/dCols
    // 4. 准备 prev 回溯数组（int **prevR, **prevC）
    // 5. while (!isEmpty(q))
    //      dequeue → 检查是否终点
    //      四个方向：墙通 + 没越界 + 未访问 → 记录 prev，入队
    // 6. 从终点顺着 prev 回溯到起点，标记路径
    // 7. 释放 prev 数组
}
```

### 三、GUI 渲染（raylib）

#### main.c 骨架

```c
#include "raylib.h"
#include "maze.h"

int main(void) {
    srand(time(NULL));

    // 窗口
    InitWindow(LENGTH, WIDTH, "Maze Explorer");
    SetTargetFPS(60);

    // 创建迷宫 + 生成
    Maze *maze = createMaze(ROWS, COLS);
    generateRandomizedMaze(maze);

    // 主循环
    while (!WindowShouldClose()) {
        BeginDrawing();
        ClearBackground(背景色);

        drawMaze(maze);       // gui.c 提供

        EndDrawing();
    }

    destroyMaze(maze);
    CloseWindow();
    return 0;
}
```

#### gui.c — 画迷宫

**1. 计算偏移量让迷宫（或按钮组）居中**：

```
居中 = (总空间 - 总内容) / 2

迷宫：OFFSET_X = (LENGTH - cols * CELL_SIZE) / 2
按钮：startY  = (WIDTH - (btnH * 3 + gap * 2)) / 2
```

每个格子的像素坐标：`pixelX = OFFSET_X + col × CELL_SIZE`，`pixelY = OFFSET_Y + row × CELL_SIZE`

**2. 画单面墙 — `DrawLine`**：

raylib 里画线就是一行调用：

```c
DrawLine(startX, startY, endX, endY, color);
```

**一个格子的四面墙分别怎么画**（设格子左上角坐标 `x, y`）：

```
       DrawLine(x, y,  x+CELL_SIZE, y)     ← top（上边，从左到右）
       DrawLine(x, y+CELL_SIZE,  x+CELL_SIZE, y+CELL_SIZE)  ← bottom
       DrawLine(x, y,  x, y+CELL_SIZE)     ← left（左边，从上到下）
       DrawLine(x+CELL_SIZE, y,  x+CELL_SIZE, y+CELL_SIZE)  ← right
```

**3. `drawMaze` 完整流程**：

```c
#include "raylib.h"

void drawMaze(Maze *maze) {
    for (int row = 0; row < maze->rows; row++) {
        for (int col = 0; col < maze->cols; col++) {

            int x = OFFSET_X + col * CELL_SIZE;  // 格子左上角 x
            int y = OFFSET_Y + row * CELL_SIZE;  // 格子左上角 y

            Cell cell = maze->grid[row][col];

            // 画格子底色（区分起点/终点/普通）
            // 画四面墙（if 有墙则画线）
        }
    }
}
```

**4. 画格子和墙的伪代码**：

```
for 每行:
    for 每列:
        计算 (x, y)

        // 先画格子填充（底色）
        if 当前是起点 (0,0):      DrawRectangle(x, y, CELL_SIZE, CELL_SIZE, GREEN)
        else if 当前是终点:       DrawRectangle(x, y, CELL_SIZE, CELL_SIZE, RED)
        else:                     DrawRectangle(x, y, CELL_SIZE, CELL_SIZE, 深灰)

        // 再画墙（画在格子底色上面）
        if cell.top:    DrawLine(x, y,  x+CELL_SIZE, y,  WALL_COLOR)
        if cell.bottom: DrawLine(x, y+CELL_SIZE,  x+CELL_SIZE, y+CELL_SIZE,  WALL_COLOR)
        if cell.left:   DrawLine(x, y,  x, y+CELL_SIZE,  WALL_COLOR)
        if cell.right:  DrawLine(x+CELL_SIZE, y,  x+CELL_SIZE, y+CELL_SIZE,  WALL_COLOR)
```

**5. raylib 颜色怎么用**：

```c
// raylib 内置颜色直接用名字：
RAYWHITE, LIGHTGRAY, GRAY, DARKGRAY, BLACK
RED, GREEN, BLUE, YELLOW, GOLD, ORANGE, PURPLE

// 自定义颜色（RGBA，每通道 0-255）：
Color wallColor = { 200, 200, 200, 255 };   // 浅灰墙
Color bgColor   = { 30,  30,  30,  255 };   // 深色背景
```

**6. 画布顺序很重要**：

先画的在底层，后画的在上层。顺序应该是：
1. `ClearBackground(背景色)` — 清屏
2. 画所有格子的底色块（`DrawRectangle`）
3. 画所有墙线（`DrawLine`）
4. （后续 BFS 阶段）画路径高亮

如果先画墙再画格子，格子会把墙盖掉。

**7. 起点终点区分**：

- 起点标记：`(0, 0)` — 绿色填充
- 终点标记：`(maze->rows - 1, maze->cols - 1)` — 红色填充

走完一趟 BFS 后再在上面叠一层路径线即可。

**8. 函数需不需要返回值**：

`drawMaze` 只画不计算，返回 `void` 即可。它只管从 `Maze *maze` 读数据、画到屏幕上。

**9. 画 BFS 路径（连线法）**：

回溯完后 `maze->path` 存了路径上每个格子的坐标，`maze->pathLen` 是长度。用格子中心连线画路径：

```c
for (int i = 0; i < maze->pathLen - 1; i++) {
    // 路径上相邻两格的正中央像素坐标
    int x1 = OFFSET_X + maze->path[i].col * CELL_SIZE + CELL_SIZE / 2;
    int y1 = OFFSET_Y + maze->path[i].row * CELL_SIZE + CELL_SIZE / 2;
    int x2 = OFFSET_X + maze->path[i+1].col * CELL_SIZE + CELL_SIZE / 2;
    int y2 = OFFSET_Y + maze->path[i+1].row * CELL_SIZE + CELL_SIZE / 2;
    DrawLine(x1, y1, x2, y2, GOLD);
}
```

每个格子中心 = `偏移 + 第几行/列 × 格子尺寸 + 半个格子（到中心）`。

注意 `path` 是终点→起点反序，遍历 `0 → pathLen-1` 就是终点画回起点，视觉效果一样。

连线法不填色、不盖墙，一条金线从起点蜿蜒到终点。

## v1.5 计划 — 算法可视化动画（已完成）

### v1.5 — 核心目标

把一次性算完的 DFS 生成和 BFS 寻路，改成**每帧只执行一步**，让用户看到算法"在动"。

---

### 引导第一步：先理解 v1 和 v2 的本质区别

抛三个问题自己先想：

1. v1 的 `generateRandomizedMaze` 用一个 while 循环一口气跑到底。v2 要逐帧推进——那个 while 循环怎么办？是删掉还是在每帧里执行一次循环体？

2. 栈/队列在 v1 里是函数局部变量，函数返回就销毁了。v2 每帧都要接着上次的状态继续——栈/队列放哪？还放局部变量行吗？

3. 状态怎么管？程序需要知道"现在我该干嘛"——是正在生成中、生成完了等人按键、还是在寻路中。需要几个状态？

---

### 引导第二步：状态机

画四个格子在纸上：
- **IDLE** —— 刚启动，什么都没做
- **GENERATING** —— 正在逐帧 DFS 生成
- **GENERATED** —— 生成完毕，等待用户按键
- **SOLVING** —— 正在逐帧 BFS 寻路

流转规则：

```
IDLE ——→ GENERATING ——→ GENERATED ——→ SOLVING
  ↑         (生成完)      (空格触发)       (BFS 结束)
  └──────────────────────────────────────────┘
                (R 键重置)
```

问自己：这个状态放哪？`main.c` 里一个局部变量，还是 `Maze` 结构体的一个字段？

---

### 引导第三步：栈/队列"长生命周期"

v1 中：

```c
void generateRandomizedMaze(Maze *maze) {
    StackNode *stack = initStack();   // 局部变量，函数返回就没了
    // ... while 循环一次性跑完
}
```

v2 需要栈**活着跨过多帧**。栈放在 `Maze` 结构体里：`StackNode *genStack;`

同样，BFS 的队列也需要持久化：`Queue *bfsQueue;`

```c
typedef struct MAZE {
    // ... 原有字段 ...
    StackNode *genStack;   // DFS 动画栈（跨帧持久）
    Queue *bfsQueue;       // BFS 动画队列（跨帧持久）
} Maze;
```

问自己：栈/队列应该在什么时候初始化？在 `createMaze` 里创建，还是按 R 键时创建？

---

### 引导第四步：`stepGenerate()` — DFS 的一帧

v1 的 while 循环体是一次完整的迭代（peek→收集邻居→洗牌→拆墙→push）。v2 里 `stepGenerate` 每帧只跑**一次迭代**。

骨架：

```c
bool stepGenerate(Maze *maze) {
    // 如果栈空 → 生成完毕，return true
    // peek 栈顶 = curr
    // 收集未访问邻居
    // 如果 count == 0 → pop → return false
    // Fisher-Yates 洗牌，chosen = unvisited[0]
    // 重新计算 nextRow/nextCol
    // 拆墙
    // 标记 visited，push 邻居
    // return false（还未完成）
}
```

问自己：
- 返回值 `bool` 的含义？true 和 false 各代表什么？
- 如果 `count == 0`，pop 后当前格还没画出来怎么办——是否需要额外标记当前帧处理的格子？

---

### 引导第五步：`stepSolve()` — BFS 的一帧

同样，每帧只**执行一次 while 循环体**（dequeue 一个节点，处理四个方向）。

```c
bool stepSolve(Maze *maze) {
    // 如果队列空 → 无解，return true
    // dequeue 一个节点
    // 如果当前是终点 → return true
    // 四个方向检查（墙通 + 未访问）
    //   通过 → 记录 prev，标记 visited，enqueue
    // return false
}
```

问自己：BFS 回溯怎么画？是一步到位画完路径，还是边搜边画已探索区域？

---

### 引导第六步：`main.c` 帧循环改造

每帧根据状态决定做什么：

```
if (state == GENERATING)
    调用 stepGenerate(maze)
    如果返回 true → state = GENERATED

if (state == SOLVING)
    调用 stepSolve(maze)
    如果返回 true → 回溯画路径 → state = SOLVED
```

按键不再直接调用全量算法，而是**切换状态**：

```
Space 键:
  如果是 GENERATED → 初始化 bfsQueue，重置 visited，起点入队 → state = SOLVING
  如果是 SOLVED → 什么都不做

R 键:
  初始化 genStack
  重置所有格子和 path
  state = GENERATING
```

问自己：Space 键触发寻路之前，是否需要先 `free` 掉上次的 `maze->path`？

---

### v1.5 改动面一览

| 文件 | 改动 |
|------|------|
| `maze.h` | 加 `genStack`、`bfsQueue`、`state` 字段；声明 `stepGenerate`、`stepSolve` |
| `maze.c` | `createMaze` 初始化 state=IDLE |
| `generate.c` | 新增 `stepGenerate()`——从 `generateRandomizedMaze` 的 while 循环拆出来 |
| `solver.c` | 新增 `stepSolve()`——从 `mazeSolver` 的 while 循环拆出来 |
| `gui.c` | 加动画提示文字（"Generating..."/"Solving..."） |
| `main.c` | 状态机驱动帧循环，按键切状态 |

---

### v1.5 新增关键提醒

- **动画速度**：太快看不清、太慢急躁。用帧计数器控制：每 N 帧执行一步（`if (frameCount++ % speed == 0) stepGenerate(maze);`）
- **速度选项**：加个按键调速（1/2/3/4 档），`speed` 变量控制 N
- **当前格高亮**：`stepGenerate` 里正在处理的格子和刚开通的邻居用特殊颜色标记，画一帧就消失——视觉上看到算法"在挖"
- **BFS 前沿**：已入队但未探索的格子用浅蓝标记，展示 BFS 的"波前扩散"
- **单步模式**：按某个键（如 `S`）手动单步，每次按一下执行一步——对答辩演示极有价值

---

### 建议开发顺序 — v1.5

1. 在 `Maze` 加 `genStack`/`state`，`createMaze` 初始化
2. 写 `stepGenerate`，`main.c` 用状态机驱动自动生成动画
3. 加当前格高亮
4. 加生成速度控制
5. 同样方式改造 `stepSolve`，BFS 动画
6. 加 BFS 探索前沿可视化
7. 加单步模式

先把前两个搞定，能看到生成动画就成功一半了。

---

## v2.0 计划 — 交互增强

### v2.0.1 — 玩家手动走迷宫（PLAYER 模式）

**目标**：迷宫生成完后，用户可以用键盘方向键自己走迷宫，从起点走到终点。

**引导问题**：

1. 玩家当前位置需要存什么？猜猜加在 `Maze` 还是 `Cell` 里？
2. 按 ↑ 时，能直接走吗？需要检查什么？
3. 走到终点怎么反馈？弹个"YOU WIN"还是放个音效？
4. 走到死胡同走不动了，用户想回头看 BFS 最短路径——"提示"按钮怎么加？
5. 计步器——走一步加一，走到终点显示步数。放哪？

**需要改的地方**：

| 文件 | 改动 |
|------|------|
| `maze.h` | Cell 或 Maze 加玩家位置、步数；加新状态 `PLAYING` |
| `gui.c` | 画玩家角色（一个高亮方块或圆点） |
| `main.c` | 在 `PLAYING` 状态下读方向键输入，逐格移动 |

---

---

#### v2.0.1 进度更新

- [x] 玩家位置 + 步数存 Maze（playerRow/playerCol/playerStep）
- [x] P 键进入 PLAYING 模式，WASD+方向键双控
- [x] 越界 + 墙检查（四方向各自判断对应墙）
- [x] 走到终点弹 YOU WIN，对比玩家步数 vs BFS 最短步数
- [x] H 键提示——静默跑 BFS 画金线路径
- [x] 随机破环——在原完美迷宫基础上随机多打通 1-2% 内部墙，增加岔路和迷惑性

#### 随机破环原理

DFS 生成的完美迷宫只有唯一路径。答辩时想更有挑战性——生成完后随便断几堵内部墙，打破"完美迷宫"结构：

```c
// 生成完成后（state = GENERATED 那一刻）
int extras = (maze->rows * maze->cols) / 50;  // 约 1-2%
for (int k = 0; k < extras; k++) {
    int r = rand() % (maze->rows - 2) + 1;    // 不碰边界行
    int c = rand() % (maze->cols - 2) + 1;    // 不碰边界列
    int dir = rand() % 4;                      // 随机一个方向
    // 拆墙（对称）+ 检查邻居不越界
}
```

加了环后迷宫不再是完美迷宫，BFS 照样能找到最短路径，手动走时更容易迷路——更有挑战性。

### v2.0.3 — 菜单系统（已完成）

**状态**：主菜单已实现，按钮式交互，`R` 重新生成、`M` 回菜单。

**实现方案**：raylib 手绘按钮（矩形 + 文字 + 鼠标碰撞检测），无外部 UI 库。

**主菜单**（启动时）：

```
Animated Generation  → 可视化 DFS+BFS 动画
Instant Generation   → 一键出迷宫
Player Exploration   → 选难度 → 手动走迷宫
```

**数据结构**：

```c
typedef struct {
    Rectangle bounds;     // {x, y, w, h}
    const char *label;
    MazeState target;     // 点后切到哪个状态
    bool hovered;         // 鼠标悬停高亮
} MenuButton;
```

**二级菜单**（Player Exploration）：三档大小 `Small(20×26)` `Medium(40×52)` `Large(60×78)`

---

### v2.0.4 — 难度选择（已完成）

**目标**：点 `Player Exploration` 后弹出大小选择界面，选完再进 PLAYING。

| 难度 | 行×列 | CELL_SIZE | 特点 |
|------|-------|-----------|------|
| Small | 20×26 | 46 | 格子大，适合快速体验 |
| Medium | 40×52 | 30 | 当前大小（默认） |
| Large | 60×78 | 20 | 格子小，高难度 |

**实现**：新状态 `SELECTING_SIZE`，新字段 `int cellSize` 存动态格大小。原先的 `ROWS`/`COLS`/`CELL_SIZE` 宏退化，改在 `createMaze` 时按难度动态分配。

**改动面**：
- `maze.h`：枚举加 `SELECTING_SIZE`，Maze 加 `cellSize`
- `gui.c`：画三个大小按钮
- `main.c`：按钮 2 切 `SELECTING_SIZE`，选完后 `createMaze(rows, cols)` 动态重建

---

### v2.0.5 — 速度控制（待定）

**引导问题**：

动画太快或太慢——怎么让用户调？加几个键，比如：

- `1` → 慢速（每 5 帧跑一步）
- `2` → 中速（每帧跑 2 步）
- `3` → 快速（每帧跑 8 步）
- `4` → 极速（每帧跑 20 步）

`Maze` 里加 `int animSpeed;` 存当前档位，`main.c` 帧循环里 `for (int i = 0; i < maze->animSpeed; i++)` 控制每帧步数。

---

### 后续想法

- **着色热力图**：BFS 探索过的格子按距离起点远近深浅着色（更酷的视觉）
- **DFS 动画显示回溯**：pop 时把格子短暂亮一下（变色一帧），展示算法"找了一圈退回来"
- **路径回溯动画**：像 BFS 探索一样，终点一格一格亮回来连成金线
- **导出/截图**：按 `S` 截图保存迷宫 + 路径

---

### 优先顺序建议

你课设答辩前，**玩家手动走迷宫**最实用——交互感强、答辩时有"人 vs AI"对比、操作直观。速度控制和选项卡是锦上添花，有时间再搞。
